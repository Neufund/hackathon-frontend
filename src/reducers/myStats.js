const initialState = {
  loading: false,
  address: null,
  neumarkAmmount: null,
};

export default function (state = initialState, action) {
  switch (action) {
    default:
      return state;
  }
}
