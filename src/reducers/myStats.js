const initialState = {
  loading: true,
  address: null,
  neumarkAmmount: null,
};

export default function (state = initialState, action) {
  switch (action) {
    default:
      return state;
  }
}
