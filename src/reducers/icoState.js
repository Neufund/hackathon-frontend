const initialState = {
  loading: true,
  raised: null,
  investorNumber: null,
  neumarkAmmount: null,
  neumarkToEtherRatio: null,
};

export default function (state = initialState, action) {
  switch (action) {
    default:
      return state;
  }
}
